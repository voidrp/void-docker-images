name: VOID Community Staff Panel

on:
  push:
    branches: [ "panel" ]
  pull_request:
    branches: [ "panel" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)

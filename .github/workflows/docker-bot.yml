name: VOID Community Discord Bot

on:
  push:
    branches: [ "bot" ]
  pull_request:
    branches: [ "bot" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
